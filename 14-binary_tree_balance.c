#include "binary_trees.h"
/**
 * binary_tree_balance - measure the balance of a binary tree
 * @tree: pointer to the root of the tree
 *
 * Return: balance, 0 if the tree is null
 */
int binary_tree_balance(const binary_tree_t *tree)
{
	int balance;

	if (tree == NULL)
		return (0);
	balance = 1;

	return (balance);
}
